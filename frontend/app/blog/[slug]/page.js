import { serverRequest } from "../../(service)/api-provider";

export default async function Article() {
  // const aa = await serverRequest("article", ``);

  return (
    <>
      <div className="py-5">
        Data:
        <br />
      </div>
    </>
  );
}

{
  "collectionName": "components_product_options",
  "info": {
    "displayName": "option",
    "description": ""
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "enumeration",
      "enum": ["QUANTITY", "WEIGHT", "SIZE", "PACKAGING", "COLOR", "FLAVOR", "TYPE", "SCENT"],
      "required": true
    },
    "value": {
      "type": "string",
      "required": true,
      "unique": false,
      "minLength": 1,
      "maxLength": 20
    }
  }
}
